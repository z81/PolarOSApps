#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//bin/sh" ]; then
  "$basedir//bin/sh"  "$basedir/../es6-transpiler/build/es5/es6toes5" "$@"
  ret=$?
else 
  /bin/sh  "$basedir/../es6-transpiler/build/es5/es6toes5" "$@"
  ret=$?
fi
exit $ret
